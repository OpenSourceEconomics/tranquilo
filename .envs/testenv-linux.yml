---
name: tranquilo
channels:
  - conda-forge
  - nodefaults
dependencies:
  - jax
  - pygmo
  - pip  # dev, tests, docs
  - pytest  # dev, tests
  - pytest-cov  # tests
  - pytest-xdist  # dev, tests
  - click  # run, tests
  - estimagic  # run, tests
  - numba  # run, tests
  - numpy>=1.17.0  # run, tests
  - pandas  # run, tests
  - plotly  # run, tests
  - scipy>=1.2.1  # run, tests
  - pip:  # dev, tests, docs
      - kaleido  # dev, tests
      - -e ../
